// Slot `@modal` sem correspondência: em carga direta ou F5 a interceptação não
// acontece, e sem este arquivo o Next renderiza 404 no lugar do slot.
export default function Default() {
  return null;
}
